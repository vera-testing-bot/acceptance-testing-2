"""Trivial module so an acceptance shard repo has code to change."""


def add(left: int, right: int) -> int:
    """Return the sum of two integers."""
    # Add left and right using Python's built-in + operator.
    return left + right
